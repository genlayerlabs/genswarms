# Tic-Tac-Toe Example

Tests objects (non-agentic components) in the swarm.

Two agents (`player_x`, `player_o`) play tic-tac-toe through a `game` object. The object validates moves, manages turns, and detects wins/draws.

## Run

```bash
mix swarm up examples/tic-tac-toe/tic_tac_toe_swarm.exs
```
